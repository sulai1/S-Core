# Core runtime dependencies for AudioGrabber Python worker.
# Pin exact versions here; bump intentionally after canary validation.
# See: python/canary.py for health-check that validates these still work.

yt-dlp==2025.4.30
requests==2.32.3
isodate==0.7.2
pandas==3.0.3
imageio-ffmpeg==0.6.0
mutagen==1.47.0
